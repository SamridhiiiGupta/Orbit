# ── Local dev: Vite proxy handles /api → localhost:5000
# VITE_API_URL is only needed if NOT using the Vite proxy (e.g. production)

# Production (fill in your Railway backend URL):
# VITE_API_URL=https://your-orbit-backend.railway.app
